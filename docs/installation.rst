============
Installation
============

At the command line::

    $ easy_install django-oscar-ficta

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-oscar-ficta
    $ pip install django-oscar-ficta
